/** Garante que `ng test` padrão tem inputs após excluir `*.integration.spec.ts`. */
describe('suite de testes', () => {
  it('carrega o ambiente de testes', () => {
    expect(true).toBe(true);
  });
});
